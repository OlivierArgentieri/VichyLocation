{% extends 'base_admin.html.twig' %}

{% block page_wrapper %}
    <h1>Renting</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ renting.id }}</td>
            </tr>
            <tr>
                <th>Beggindate</th>
                <td>{% if renting.begginDate %}{{ renting.begginDate|date('Y-m-d') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Enddate</th>
                <td>{% if renting.endDate %}{{ renting.endDate|date('Y-m-d') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin-panel_renting_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin-panel_renting_edit', { 'id': renting.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock page_wrapper%}
